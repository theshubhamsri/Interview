package Enum;
//Only one snapshot of enum exists per JVM
//By default the constructor of ENUM is private
public enum EnumSample {
    MONDAY,
    TUESDAY,
    WEDNESDAY,
    THURSDAY,
    FRIDAY,
    SATURDAY,
    SUNDAY;
}
